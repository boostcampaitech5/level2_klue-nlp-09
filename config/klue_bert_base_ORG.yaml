# model
model_name: klue/bert-base

# data
train_path: ../dataset/train/train_90_ORG.csv
dev_path: ../dataset/dev/dev_10_ORG.csv
predict_path: ../dataset/test/test_ORG.csv

# seed
seed: 42

# train
output_dir: ./results  # output directory
save_total_limit: 5  # number of total save model.
save_steps: 300  # model saving step.
num_train_epochs: 10  # total number of training epochs
learning_rate: 1e-6  # learning_rate
per_device_train_batch_size: 16  # batch size per device during training
per_device_eval_batch_size: 16  # batch size for evaluation
warmup_steps: 0  # number of warmup steps for learning rate scheduler
weight_decay: 0.01  # strength of weight decay
logging_dir: ./logs  # directory for storing logs
logging_steps: 100  # log saving step.
evaluation_strategy: steps  # evaluation strategy to adopt during training
                            # `no`: No evaluation during training.
                            # `steps`: Evaluate every `eval_steps`.
                            # `epoch`: Evaluate every end of epoch.
type: ORG # PER or ORG
eval_steps: 300  # evaluation step.
load_best_model_at_end: True

# inference
inference_model_name: ORG-epoch_9-micro f1 score_79.25

# save
save_path: ./model/per_

# wandb
run_name: org
project_name: klue